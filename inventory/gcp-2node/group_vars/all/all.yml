---
# Minimal overrides for gcp-2node inventory
# Enforce 'external' cloud provider handling per guidance (do not install cloud controllers)
cloud_provider: external
# Explicitly set external_cloud_provider to 'manual' to satisfy Kubespray validation
# and to ensure no cloud controller is installed by Kubespray.
external_cloud_provider: manual
